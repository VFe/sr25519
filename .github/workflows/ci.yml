name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least privilege: CI only reads the repo.
permissions:
  contents: read

jobs:
  test:
    name: conformance (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, full: true }
          - { os: macos-latest, full: true }
          # Windows runs the portable subset: test_helper.exs excludes the
          # :posix_build tests there by itself.
          - { os: windows-latest, full: false }
    env:
      SR25519_FORCE_BUILD: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Elixir/OTP
        uses: erlef/setup-beam@54075bcc5e249e4758d363f27d099f55d843f124 # v1.24.1
        with:
          otp-version: "26.2"
          elixir-version: "1.16.3"

      # SHA-pinned to master (no semver tags on this action); normally the ref
      # selects the toolchain, so with a SHA pin the toolchain must be explicit.
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88 # master
        with:
          toolchain: stable

      - name: Cache cargo registry and NIF target
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          workspaces: native/sr25519_nif

      # The key embeds the OTP/Elixir versions because dialyzer PLT filenames
      # do: a toolchain bump with an unchanged mix.lock must not hit a cache
      # whose PLTs the new toolchain cannot reuse.
      - name: Cache mix deps, build, and dialyzer PLTs
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            deps
            _build
            priv/plts
          key: mix-${{ runner.os }}-otp26.2-elixir1.16.3-${{ hashFiles('mix.lock') }}
          restore-keys: mix-${{ runner.os }}-otp26.2-elixir1.16.3-

      - name: Reject panic = "abort" (BEAM VM-safety guard)
        shell: bash
        run: |
          if grep -RqsE 'panic *= *"abort"' native/*/Cargo.toml native/*/.cargo/; then
            echo "::error::panic = \"abort\" would make a NIF panic kill the VM"; exit 1
          fi
          grep -qE 'panic *= *"unwind"' native/sr25519_nif/Cargo.toml || {
            echo "::error::[profile.release] panic = \"unwind\" is required"; exit 1; }

      - name: Lockfile integrity (reproducible native build)
        shell: bash
        run: |
          # rustler does not pass --locked to cargo, so builds silently consume
          # whatever Cargo.lock resolves to; this guard fails the build if the
          # committed lockfile is stale relative to Cargo.toml.
          cargo metadata --locked --format-version 1 --manifest-path native/sr25519_nif/Cargo.toml > /dev/null
          cargo metadata --locked --format-version 1 --manifest-path vectors/rust_oracle/Cargo.toml > /dev/null

      - name: Fetch deps
        run: mix deps.get

      - name: Check formatting
        if: matrix.os == 'ubuntu-latest'
        run: mix format --check-formatted

      - name: Conformance ladder (full)
        if: matrix.full
        run: mix conformance

      - name: Dialyzer
        if: matrix.os == 'ubuntu-latest'
        run: mix dialyzer --format github

      - name: Tests (portable subset)
        if: ${{ !matrix.full }}
        run: mix test

      - name: Upload conformance report
        if: ${{ always() && matrix.full }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: conformance-report-${{ matrix.os }}
          path: conformance_report.json
          if-no-files-found: ignore
